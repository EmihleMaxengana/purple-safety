import 'package:flutter/material.dart';

class SafetyActionsGrid extends StatelessWidget {
  const SafetyActionsGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
